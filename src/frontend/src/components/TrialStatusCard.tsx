// Trial status card is disabled — useGetTrialStatus is not available in this version.
// This component renders nothing to avoid breaking any imports.
export default function TrialStatusCard() {
  return null;
}
