import { AlertTriangle, Clock } from 'lucide-react';

// Trial status banner is disabled — useGetTrialStatus is not available in this version.
// This component renders nothing to avoid breaking the AdminLayout import.
export default function TrialStatusBanner() {
  return null;
}
